# 이 값은 Vercel 프로젝트의 환경변수에만 넣는다. 실제 키를 커밋하지 말 것.
ANTHROPIC_API_KEY=

# Supabase 공유 저장 (M2). 없으면 이 기기에만 저장하는 모드로 동작한다.
# anon 키는 브라우저에 노출돼도 되는 값이다 — 통제는 RLS가 한다.
# service_role 키는 절대 여기 넣지 않는다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
