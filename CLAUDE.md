# 포클로 매입·부가세 관리 웹앱 — 개발 지시서

> 이 문서를 Claude Code 첫 프롬프트로 붙여넣어 주세요. 프로젝트 루트에 `CLAUDE.md`로 저장해두면 이후 세션에서도 계속 참고됩니다.

## 시작 지시

이 문서를 읽고 아래 웹앱을 만들어줘. 시작 전에 내 환경만 먼저 확인해줘: Node.js 버전, GitHub 계정 유무, 배포에 쓸 Vercel(또는 Netlify) 계정, 공유 데이터에 쓸 Supabase 계정, 영수증 인식에 쓸 Anthropic API 키. 준비 안 된 게 있으면 무료로 만드는 법을 알려주고, **마일스톤 M1(로컬 저장으로 먼저 동작 + 배포)부터** 진행해줘. 참고용 UI/로직은 같은 폴더의 `poclo-vat-ledger.jsx`에 있어.

---

## 0. 무엇을, 왜

포클로(여성의류 이커머스)의 **사입 매입 장부** 웹앱. 현재 간이과세자이고 내년 7월 1일 일반과세자로 전환 예정. 도매 사입을 건 단위로 기록하되, **삼촌 현금대납(부가세 미포함)**과 **직접이체(부가세 포함)**를 구분해 자동 집계하는 게 핵심 목적. 운영자 2명(나 + 지원)이 **같은 장부를 공유**하고, 폰 브라우저에서 홈 화면에 추가(PWA)해 쓴다.

## 1. 핵심 도메인 규칙 (이 앱의 존재 이유)

- 거래는 '거래처'가 아니라 **'거래 건' 단위**로 저장한다. 한 거래처 안에 이체 건과 대납 건이 섞일 수 있다("처음엔 대납, 상품 터지면 이체 전환").
- 결제방식은 2가지:
  - **transfer(이체)**: 부가세 포함 지급. 부가세 = 공급가액 × 10% → "낸 부가세".
  - **samchon(삼촌대납)**: 부가세 미포함 현금 지급. 부가세 안 냄 → **"미증빙 매입"**.
- **전환비용**: 대납(미증빙) 건을 세금계산서로 돌리려면 추가로 낼 부가세 = 대납 공급가액 × 10%. 앱이 항상 합계로 보여준다.
- 세금계산서 수취 여부를 **건별**로 기록한다.
- **세금계산서 우선순위 하이라이트**(과세유형 토글에 따라 규칙이 바뀜):
  - **간이과세** 모드: 거래처 총매입 ≥ 300,000원이면서 (대납분이 있거나 세금계산서 미수취 건이 있으면) 하이라이트.
  - **일반과세** 모드: 대납분이 있거나 세금계산서 미수취 건이 있는 거래처 전부 하이라이트.
- (배경 지식 — 코드 주석/문구로만 남기고 세무 조언은 하지 말 것) 간이 기간엔 세금계산서를 받아도 부가세 공제 실익이 거의 없고, 일반 전환 후엔 매입세액 10%가 전액 공제된다. 그래서 "지금은 큰 거래처만, 전환 후엔 전부"라는 판단을 **과세유형 토글**로 조작한다.

## 2. 데이터 모델

```
Transaction {
  id: string
  date: string          // 'YYYY-MM-DD'
  vendor: string        // 거래처(상호)명
  supply: number        // 공급가액 (부가세 별도, 원 단위 정수)
  method: 'transfer' | 'samchon'
  invoice: boolean      // 세금계산서 수취 여부
  memo: string          // 품목/메모
}
```
파생값: `vat = supply * 0.1`, `paidVat = method==='transfer' ? vat : 0`, `pendingVat = method==='samchon' ? vat : 0`, `actualPaid = method==='transfer' ? supply+vat : supply`.

## 3. 화면 구성

- **상단 KPI**: ① 미증빙 대납 총액(가장 강조) ② 전환 시 추가 부가세 ③ 총매입 ④ 낸 부가세 ⑤ 세금계산서 수취율.
- **액션 버튼**: `영수증 올리기`(카메라/이미지 → OCR 자동입력), `직접 추가`.
- **탭 2개**:
  - **일별**: 날짜별로 묶어 하루 합계 표시. 각 건은 이체/대납 토글 · 세금계산서 체크 · 삭제 가능.
  - **거래처별**: 거래처마다 총매입 / 이체분(증빙) / 대납분(미증빙) / 전환비용 / 세금계산서 n·N / 혼합 여부 뱃지 / 우선순위 점.
- **월 선택 드롭다운**, **간이/일반 토글**을 상단에 둔다.
- 한국어 UI, 원화 `₩` 콤마 표기, 숫자 우측정렬(tabular-nums).

## 4. 영수증 OCR

- 이미지 업로드/촬영 → **서버리스 함수**에서 Anthropic API(비전 모델)로 `{ vendor, date, supply, items }`를 추출 → 입력 폼에 프리필.
- **결제방식은 영수증만으로 알 수 없다.** 반드시 사용자가 이체/대납을 직접 고르게 하고, **저장 전 확인 단계**를 거친다(손글씨 명세서 오독 대비).
- **Anthropic API 키는 절대 프런트엔드에 노출 금지.** 서버리스 함수의 환경변수로만 사용.
- OCR 실패 시 빈 폼으로 열고 "직접 입력" 안내(사과 문구 대신 다음 행동 안내).

## 5. 기술 스택 (권장 — 더 나은 안 있으면 제안해도 됨)

- 프런트: **React + Vite + Tailwind**, PWA(홈 화면 추가) 설정.
- 공유 데이터 + 로그인: **Supabase**(무료 티어). 나와 지원이 같은 데이터 조회.
- OCR: **Vercel Serverless Function** 또는 **Supabase Edge Function**에서 Anthropic API 호출.
- 배포: **Vercel** 권장(GitHub 연동 → push 시 자동 배포). URL 발급 후 폰 홈 화면 추가.

## 6. 마일스톤

- **M1**: 프로젝트 셋업 + 기존 UI/로직 이관(우선 브라우저 로컬 저장으로 동작) + Vercel 배포까지. → 바로 URL로 확인 가능.
- **M2**: Supabase 연결 — 공유 저장 + 간단 로그인.
- **M3**: 영수증 OCR을 서버리스로 옮겨 안전화.
- **M4**: CSV 일괄등록 / 삼촌 대납 정산(갚을 현금 합계) / 매출·예상 부가세.

각 마일스톤 끝날 때마다 배포해서 URL로 바로 확인할 수 있게 해줘.

## 7. UX·카피·톤

- 톤: 포클로(여성의류 브랜드) — 깔끔하고 차분하게. 로즈 계열 포인트 컬러, 나머지는 뉴트럴(스톤).
- 이체=초록 계열, 대납=앰버 계열로 의미 구분.
- 빈 화면/오류는 다음 할 일을 안내(사과·모호한 문구 금지).
- 모바일 우선. 폰 한 손 사용 기준으로 버튼·글자 크기 확보.

## 8. 하지 말 것

- 세무 상담·조언 텍스트를 앱에 넣지 말 것(기록·집계·표시 도구일 뿐).
- API 키/시크릿을 클라이언트 코드나 저장소에 커밋하지 말 것.
- 영수증 OCR 결과를 확인 없이 자동 저장하지 말 것.

---

## 9. 현재 구현 상태 (2026-09-04)

**M1 완료 · M3(OCR 서버리스) 선반영.**

- 스택: React 19 + Vite 8 + Tailwind 4 + `vite-plugin-pwa`, `lucide-react`.
- 저장: 브라우저 `localStorage` (`poclo_purchases_v1`). 읽기/쓰기는 `src/lib/storage.js`
  한 곳에만 있으므로 M2에서 Supabase로 갈아끼울 때 이 파일만 바꾸면 된다.
- 도메인 계산(`derive` / `groupByDay` / `groupByVendor` / `totals`)은 전부
  `src/lib/calc.js`에 모여 있고 UI와 분리돼 있다.
- 장끼(영수증) OCR: `api/read-receipt.js` (Vercel Serverless Function). `claude-opus-5`
  + structured outputs, effort high. **`ANTHROPIC_API_KEY`는 이 함수의 환경변수로만
  존재한다.** 프론트는 `/api/read-receipt`만 호출한다(`src/lib/receipt.js`).
  로컬 `npm run dev`에서는 `vite.config.js`의 devApi 플러그인이 같은 handler를
  미들웨어로 물리고 루트 `.env`에서 키를 읽는다.
- 장끼에서 읽는 항목: 거래처명·위치·전화번호·계좌번호·사업자번호·상품명·날짜·금액.
  프롬프트에 두 가지 함정이 박혀 있다 — (1) 장끼의 "거래처명: 포클로"는 매입자인
  우리이므로 vendor로 잡으면 안 되고 판 가게 상호를 찾아야 한다, (2) 종이 바깥
  배경(상표택·다른 서류)은 읽지 않는다. 프롬프트를 고칠 때 이 둘을 깨뜨리지 말 것.
- 실패 원인은 상태코드로 갈라 화면에 그대로 보여준다: 키 없음/형식오류 503,
  잔액 부족 402, 인식 실패 422. 뭉뚱그리면 사용자가 고칠 수가 없다.
- 씨앗 데이터 없음 — 실제 장부이므로 빈 상태로 시작한다.

### 데이터 구조 (v2)

거래처와 거래를 나눴다. **거래처는 이름이 아니라 고유 id로 식별한다** —
장끼마다 상호가 다르게 읽혀도("ONE PICK" / "원픽") 같은 거래처로 남아야 하므로.

```
Vendor { id, name, address, phone, bizNo, accounts:[{id,bank,number,holder}], memo }
Tx     { id, vendorId, date, items:[{id,name,unitPrice,qty,amount,pending}],
         supply, method, vatPaid, invoice, accountId, memo, hasPhoto }
```

- **결제 상태는 세 가지다** (`MODES` in calc.js). 이체를 했어도 부가세는 안 보낸
  경우가 있어서 이체가 둘로 갈린다:
  `이체+vatPaid` / `이체+!vatPaid` / `삼촌 대납`. 칩을 누르면 이 순서로 돈다.
  부가세를 냈는지는 `method`가 아니라 **`derive(t).paidVat > 0`으로 판단**할 것.
- `items[].qty`는 음수가 될 수 있다(반품·교환). 금액도 따라 음수가 되므로 `won()`은
  부호를 통화기호 앞에 붙인다.

- `supply`는 **당일합계**이고 이것이 장부 금액이다. 품목 합계와 다를 수 있으며
  (에누리 등) 그때는 당일합계를 따른다. 품목 합계는 참고용으로만 보여준다.
- `accountId`는 실제로 송금한 계좌. 계좌가 하나면 자동으로 그것이 선택된다.
- `items[].pending`이 미송.
- 장끼 사진은 localStorage(5MB)를 금방 채우므로 **IndexedDB에 따로** 둔다
  (`src/lib/photos.js`). 거래에는 `hasPhoto` 플래그만 남는다.
- 예전 v1 데이터(`poclo_purchases_v1`)는 첫 실행 때 자동으로 옮겨심고 **지우지 않는다.**

### 거래처 자동 매칭 (`src/lib/match.js`)

장끼를 읽으면 기존 거래처와 맞춰본다. 순서가 중요하다:
1. 사업자번호 · 계좌번호 · 전화번호가 같으면 **같은 거래처로 확정**한다.
   이름은 사진마다 흔들리지만 번호는 안 흔들린다.
2. 이름만 비슷하면 **후보를 보여주고 고르게** 한다. 자동으로 합치지 않는다 —
   다른 가게일 수 있다.
3. 아무것도 안 맞으면 새 거래처.

이름 비교는 괄호 안팎을 각각 조각으로 떼어 비교한다. "ONE PICK (원픽)"에서
괄호를 통째로 버리면 "원픽"과 못 만난다.

### M2 — 공유 저장 (Supabase)

`VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`가 있으면 **remote 모드**로 돌고,
없으면 예전처럼 이 기기에만 저장하는 **local 모드**로 돈다. 화면 코드는 어느
쪽인지 모른다 — `src/lib/useLedger.js`가 그 차이를 전부 흡수한다.

- anon 키는 브라우저에 노출돼도 되는 값이다. 실제 통제는 RLS가 한다
  (로그인 안 하면 아무것도 못 읽음). **service_role 키는 클라이언트에 절대 쓰지 않는다.**
- 한 쪽이 고치면 realtime으로 다른 쪽이 따라 바뀐다. 어느 행이 바뀌었는지
  따지지 않고 통째로 다시 읽는다 — 장부가 작아 그게 더 단순하고 틀릴 일이 없다.
- 스키마와 권한은 `supabase/schema.sql` 한 파일에 있다. 여러 번 실행해도 안전하다.
- 장끼 사진은 Storage의 `receipts` 버킷. local 모드에서는 IndexedDB.
- 로그인은 이메일+비밀번호. 계정은 Supabase 대시보드에서 미리 만든다(가입 화면 없음).
- 이 기기에만 있던 장부는 로그인 후 배너를 눌러 공유 장부로 올린다.

### 실시간 갱신과 화면 상태

remote 모드에서는 저장할 때마다 realtime이 울려 전체를 다시 읽는다. 이때 화면을
통째로 "불러오는 중"으로 바꾸면 보고 있던 거래처 상세가 사라졌다 다시 그려지면서
목록으로 튕긴다. 그래서 **스피너는 첫 로딩 한 번만** 띄운다
(`useLedger.js`의 `loadedOnce`). 이 부분을 고칠 때 그 이유를 잊지 말 것.

### 아직 안 한 것
- M4: CSV 일괄등록 / 삼촌 대납 정산 / 매출·예상 부가세.
- 거래처 합치기가 지금은 이름을 쳐서 고르는 방식(`window.prompt`)이다. 목록에서
  골라 합치는 화면이 있으면 낫다.

---

## 10. 매출 합치기 (2026-09-08)

**앱이 매입 전용에서 매입 + 매출로 넓어졌다.** 이름도 '포클로 매입 장부' 하나가
아니라, 왼쪽 띠에서 큰 갈래를 고르는 구조가 됐다 (쇼필공ERP 형식을 참고).

```
홈       두 장부 요약 (이번 달)
일       (아직 없음) 상품등록 대기열·촬영/보정 진행
돈       파는 쪽 → 포클로 매출 장부
         사는 쪽 → 포클로 매입 장부 / 거래처
         정산·세무 → 세금계산서 대조
사람     (아직 없음) 재구매·신규 대 재구매
콘텐츠   (아직 없음) 릴스·상세페이지·사진 감도
```

갈래 설정은 `src/lib/nav.js` 한 곳에 있다. `Sidebar.jsx`는 그리기만 한다.
안 만든 갈래는 `Soon.jsx`가 **무엇이 들어올 것인지만** 적는다 — 빈 껍데기를
그럴듯하게 두면 있는 줄 알고 헛걸음하게 된다.

### 매출을 두 단계로 나눠 보는 이유 (이게 핵심)

섞으면 둘 다 못 본다. 그래서 **화면도 나눴다.**

- **총매출** — 그날 실제로 팔린 금액. 취소·반품을 아직 안 뺐다.
  **광고비·ROAS는 여기에 대고 본다.** 광고가 만들어 낸 매출이 이것이고,
  나중에 반품되는 것은 광고 탓이 아니다. → `SalesPage` (포클로 매출 장부)
- **순매출** — 거기서 취소·반품을 뺀 것. **손익은 여기서 낸다.** 반품된
  주문에는 마진이 없다. → `PnlPage` (손익)

**매출은 전부 카페24 애널리틱스 › 매출분석에서 온다.**

    총매출 = 결제합계          (배송비 포함)
    순매출 = 결제합계 − 환불합계   ← 카페24 화면의 '순매출'과 같은 값

**주문 API로 매출을 세면 안 된다.** 네이버페이 주문의 30%가 결제액 0원으로 내려와
매출이 실제보다 낮게 잡힌다 (2026-08 기준 정가 484만원어치, 순매출이 683만원 낮게 나왔다).
주문 API는 **원가·수량·건수·네이버 비중**을 얻는 데만 쓴다 — 이 값들은 결제액과 무관하다.
이 함정에 한 번 빠져서 8월을 적자로 보고했었다. 다시 밟지 말 것.

원가율은 배송비를 뺀 **상품매출**(`goods = net − cafeShip`)에 대고 잰다.

두 원자료는 **기준일이 다르다** — 카페24는 결제완료일, 주문 API는 주문일.
하루 단위로는 어긋나고 월 단위로는 거의 맞는다. 고치려 들지 말 것, 둘 다 맞는 숫자다.

8월 기준: 총매출 42,965,713 · 광고비 9,492,384 → **광고비율 22.1%**, ROAS 4.53.
목표는 **월 18%** (`TARGET_AD_RATE`).

`PnlPage`는 **원가를 아는 날만** 손익을 낸다(`hasOrders`). 매출만 있고 주문
데이터가 없는 날을 섞으면 광고비만 빠져 적자로 보인다. `HomePage`도 같은 이유로
매출·광고는 전체 날짜로, 손익은 `hasOrders` 날짜로 따로 낸다.

### 데이터가 오는 길

앱은 카페24를 직접 부르지 않는다. `C:\Users\WD\poclo-cafe24`의 파이썬이
주문 API에서 뽑아 CSV로 내리고, 그걸 매출 장부 화면에 붙여넣거나 파일로 올린다.

- 카페24 › 애널리틱스 › 매출분석 › 일별 CSV → **총매출(결제합계)·환불합계**.
  2026-02-21부터 있다. 맨 위 안내문 여러 줄을 건너뛰고 '일자' 줄을 머리글로 찾는다.
- `orders.py` → `orders_일별.csv` — 순매출·매출원가·판매수량·확정주문·배송비·
  네이버매출. 2026-08-01부터. 원가는 **주문에 붙어 온 공급가**라 가정이 아니다.
- 메타 광고 관리자 › '일' 단위 내보내기 → 광고세트별 여러 줄이어도 날짜로 합친다.

### 씨앗 데이터

`src/lib/seed.js`에 지금까지 모은 144일(2026-02-21~09-07)과 단가표 241개가 박혀 있다.
**저장된 게 하나도 없을 때만** 들어간다 — 한 번이라도 넣었으면 안 건드린다. 서버가
비어 있으면 한 번 올려 준다. 새로 뽑으면 화면의 '데이터 넣기'로 덮으면 되고,
`seed.js`를 손으로 고칠 일은 없다 (생성기는 scratchpad의 `gen_seed.py`).

읽는 쪽은 `src/lib/sales.js` (`parseDaily` / `parseAds` / `dayPnl` / `totalPnl`).
CSV 파서는 따옴표 안 쉼표까지 지키는 최소 구현이 이 파일에 들어 있다.

### 저장

`src/lib/useSales.js`가 `useLedger`와 같은 규칙으로 돈다 —
Supabase의 `sales_daily` + `settings`, 없으면 localStorage.
**표가 아직 없으면 화면을 죽이지 않고 로컬로 계속 돈다.** 스키마를 올리면 그때 서버로 간다.
`supabase/schema.sql`을 다시 실행하면 표가 생긴다(여러 번 실행해도 안전).

### 비용 가정값

매출 장부 › 데이터 넣기 › 비용 가정값에서 고친다. 기본값은 `DEFAULT_COSTS`.

| | 값 | 비고 |
|---|---|---|
| 택배비 / 건 | 2,300 | 우리가 내는 돈. 고객에게는 3,000 받는다(주문에서 옴) |
| 부자재 / 개 | 197 | |
| 결제 수수료 | 2.0% | 네이버 외 |
| 네이버페이 | 3.74% | 부가세 포함. 주문의 38%가 네이버다 |
| 월 고정비 | 1,700,000 | 임대·앱·삼촌 |

### 아직 안 한 것

- 매입과 매출을 **한 손익으로 합치기** — 부가세(매출세액 − 매입세액), 재고
  (매입수량 − 판매수량), 현금흐름. 연결 고리는 **거래처명**이다. 카페24 상품의
  `model_name`과 매입장부 거래처명이 같은 이름을 쓴다(그랑블루·니트피아·디벨롭…).
- 삼촌비가 지금 월 고정비에 뭉뚱그려 있다. 매입장부에 '삼촌 대납'으로 이미
  건별로 쌓이고 있으므로 거기서 끌어오면 된다.
- 카페24 → 앱 직결(지금은 CSV를 사람이 옮긴다).

---

## 11. 손으로 고치는 자리 (2026-09-08)

자동으로 들어온 숫자라도 **사람이 덮어쓸 수 있어야 한다.** 실제 청구서가 계산과
다른 경우가 늘 있기 때문이다(8월 택배비가 중간에 2,600→2,300으로 바뀐 것처럼).

- **표 안의 숫자를 눌러서 고친다** (`EditNum.jsx`). 매출 장부의 총매출·광고비·환불.
  고친 값은 그대로 저장되고 다음 CSV 가져오기 때문에 다시 덮인다.
- **광고비 직접 넣기** — 날짜 고르고 금액만. CSV 없이 하루치.
- **달마다 다른 값** (`conf.monthly`) — 택배비 총액 / 삼촌비 / 고정비.
  택배비 총액을 넣으면 건당 계산 대신 그 달 건수 비율대로 나눠 담는다(`dayPnl`).
  8월 2,249,200원(부가세 포함)은 `SEED_MONTHLY`에 심어 뒀다.
- **삼촌비는 고정비에서 뗐다.** 고정비 120만(관리비 20 + 앱 100) + 삼촌비 50만.
  삼촌비는 거래처 수 × 2,000 + 배송 3,000 식이라 달마다 다르다.

## 12. 공유 상태를 늘 보여준다

상대가 고친 게 왜 안 보이는지 알 방법이 없으면 앱을 못 믿는다.

- `SyncBadge` — 공유 장부/실시간 연결됨 · 끊김 · 이 기기에만 저장. 항상 화면 위에.
- `remote.subscribe`가 채널 상태를 밖으로 알리고, **탭을 다시 볼 때 한 번 더 읽는다**.
  실시간이 조용히 끊겨도 화면을 보면 최신이 된다.
- `UploadBanner` — 예전 "공유 장부로 옮길까요?"는 어디로 가는지를 안 알려줬다.
  무엇이 몇 건, 누구 계정으로, 상대에게 어떻게 보이는지까지 적는다.

## 13. 하루 한 번 자동 갱신

`poclo-cafe24/daily.py` — 단가표 + 주문 + 매출분석을 받아 **Supabase `sales_daily`에
직접 밀어 넣는다.** 앱에서 붙여넣을 필요가 없어진다.

    6_하루갱신.bat        한 번 돌리기
    7_매일자동_등록.bat   윈도우 작업 스케줄러에 새벽 1시로 등록 (PocloDaily)

`.env`에 `SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_EMAIL / SUPABASE_PASSWORD`가
있어야 올라간다. 없으면 CSV만 만들고 끝난다(앱에서 손으로 올리면 된다).

**총매출·환불도 이제 자동이다.** 매출 통계 API는 끝내 안 열렸지만
(`mall.read_salesreport` 는 없는 스코프, `mall.read_store` 로도 403),
`poclo-cafe24/revenue.py` 가 **주문 API로 같은 숫자를 되살린다.**

    총매출(그날) = Σ (initial_order_amount.payment_amount + naver_point)
                   결제일이 그날인 주문 전부

네이버페이가 `payment_amount` 0 / `naver_point` 에 금액을 넣는 것과, 취소되면
`actual_order_amount` 가 0으로 덮이는 것 — 이 둘만 피하면 된다. 2026-08 검증에서
42,965,713원으로 **카페24 화면과 1원도 안 틀렸다.** 환불만 0.19% 모자라므로
손익을 확정할 때는 애널리틱스 CSV로 한 번 덮는 게 맞다.
**광고비는 자동이 아니다** — 메타는 계정 연동이 따로 필요해서 앱에서 넣는 쪽이 빠르다.


---

## 14. 미송 · 불량 · 매입금 · 잔액 (2026-09-09)

동대문 사입에는 "돈은 지나갔는데 안 끝난 것"이 늘 섞여 있다. 이걸 안 보면
**같은 물건 값을 두 번 낸다.** 그래서 매입 쪽에 화면 하나(`PendingPage`)와
계산 파일 하나(`src/lib/pending.js`)를 새로 뒀다.

### 품목 한 줄의 성격 (`items[].kind`)

칩 하나를 눌러 **매입 → 미송 → 출고 → 불량 → 교환** 순으로 돈다 (`KINDS` in calc.js).

| kind | 뜻 | 당일합계에 |
|---|---|---|
| `buy` | 산 물건 | 든다 |
| `pending` | 미송 — 돈은 냈고 물건은 나중에 | 든다 |
| `pendingOut` | 미송 출고 — 미송분이 도착 | **안 든다** |
| `defect` | 불량 표시 — 산 물건인데 불량이라 표시. 교환받을 때까지 **잡혀 있다** | **든다** (9/16 세원 정정) |
| `defectOut` | 교환 받음 — 잡힌 불량을 푼다. 품목명 = **받은 것**, `linkName` = 원래 불량 품목 | **안 든다** (9/16) |

**`pendingOut`·`defectOut` 을 합계에서 빼는 것이 이 기능의 존재 이유다** (`PREPAID_KINDS`).
**불량(`defect`)은 빼지 않는다** — 9/15에 뺐다가 세원이 정정했다: "불량은 그냥 매입인데 나중에 교환받을 때
확인하기 쉽게 체크해 둔 것." 합계에서 빠지는 건 **출고·교환**뿐이다. 그 물건 값은
미송을 잡던 날 이미 냈다. 여기서 안 빼면 이중 지불이 된다 (`itemsTotal`).
출고분만 받은 날은 낼 돈이 0원이므로 **금액·결제방식을 강요하지 않는다**(`onlyPrepaid`).

예전 기록에는 `kind` 가 없고 `pending` 불리언만 있었다. `makeItem` 이 그걸 읽어
`pending` → `kind:"pending"` 으로 옮긴다. 새 코드도 `pending` 불리언을 계속 채워 둔다.

### 미송 현황

거래처 × 품목으로 묶어 **잡은 수량 − 출고된 수량 = 남은 수량**. 품목명은
띄어쓰기·대소문자를 지우고 맞댄다(`itemKey`). 잡은 날과 받은 날을 다 들고 있어서
"언제 잡았더라"를 볼 수 있다. 장끼를 넣을 때 그 거래처에 남은 미송이 폼 안에 뜨고,
누르면 **출고 줄이 바로 만들어진다** — 손으로 다시 치면 이름이 어긋나 안 맞물린다.

### 불량은 미송과 같은 모양으로 흐른다 (2026-09-16)

세원: "불량건 오면 그것만 불량으로 바꿔 두고, 교환받을 때 그 거래처 불량건을 골라 어떤 품목으로 몇 장
교환받았는지 적거나 매입을 잡거나. 미송처럼 남아 있는 게 매입 장부 위에 떠 있다가 교환되면 사라지게."

- `pending.flowBoard(tx, vendorName, "pending" | "defect")` 하나로 둘 다 센다 (`FLOWS`).
  잡힌 수량 − 풀린 수량 = 남은 것. 잡은 날·풀린 날·**무엇으로 받았는지(got)**·비고를 들고 있다.
- **불량은 다른 품목으로 바꿔 받는 일이 많다.** 그래서 `defectOut` 은 이름이 아니라 `linkName` 으로
  원래 불량과 잇는다(`flowName`). 받은 품목명이 원래와 다르면 `got` 으로 "→ 비너스세트/블랙"처럼 보인다.
- 장끼 폼: 그 거래처에 남은 불량이 빨간 상자로 뜨고, 누르면 `linkName` 이 채워진 교환 줄이 생긴다.
  이름만 받은 품목으로 바꾸면 된다. 칩으로 직접 '교환'을 고르면 원래 불량을 고르는 칸이 나온다.
  **매입으로 정리**한 경우도 교환 줄로 풀고 비고에 적는다(금액은 매입금 칸).
  **고치고 있는 장끼 자신은 '남은 것'에서 뺀다** — 안 그러면 자기 불량이 자기 폼에 또 뜬다.
- 매입 장부 위: 미송(앰버)·불량(로즈) 카드에 **거래처 · 품목 · 장수 · 금액 · 언제부터**를 4줄까지.
  불량 카드를 누르면 미송·매입금 화면 **불량 탭**으로 바로 연다(`App.pendingTab`).
- 거래처 상세에도 남은 불량이 뜬다.
- 예전 `defect` 기록(9/4 오도시 시어브이넥/그레이 2장)은 그대로 '잡힌 불량'으로 읽힌다 — 옮길 것 없음.

### 잔액 (전잔 · 당일합계 · 현금입금 · 당잔)

    당잔 = 전잔 + 당일합계 − 현금입금      (장끼와 같은 부호)

**양수 = 우리가 덜 낸 돈(다음에 더 냄), 음수 = 더 낸 돈(다음에 깎음).**
3,500원짜리를 4,000원 내면 당잔 −500, 다음 날 3,500원을 3,000원만 내면 당잔 0 (세원 예시 그대로).
**2026-09-15에 부호를 뒤집었다** — 처음엔 "낸 돈 − 살 돈"(양수 = 더 냄)으로 만들었는데,
장끼 숫자를 그대로 옮겨 적으려면 장끼와 부호가 같아야 해서.

`cashPaid` 를 **안 적으면 null** 이고, 그건 "딱 맞게 냈다"는 뜻이다.
**0과 null 은 다른 뜻이다** — 출고분만 받은 날은 진짜 0원이다.

**전잔·당일합계·당잔 세 칸 모두 손으로 적을 수 있다** (세원 요청 2026-09-15). 비우면 앱이 계산한 값이
흐리게 보이고, 치면 장끼 값으로 남는다 (`prevBalance` / `balance`, 둘 다 null = 안 적음).
계산은 `pending.stepBalance` 한 곳:
- 전잔을 적으면 **그 거래부터 잔액이 장끼 값에서 다시 시작**한다. 앱이 이어 센 값과 다르면 차이를 경고한다
  (빠진 장끼·잘못 적은 숫자).
- 당잔을 적고 현금입금을 비우면 **입금을 거꾸로 푼다**. 둘 다 적었는데 안 맞으면 "장끼 숫자끼리 안 맞음" 경고.
- 다음 거래로는 적은 당잔이 넘어간다. **거래처 장부(장끼)가 기준이다.**
- 휴대폰 숫자 자판엔 − 가 없어서 칸마다 ± 단추가 있다.
당잔이 천원을 넘게 어긋나면 폼에서 노랗게 경고한다(적은 값이 틀렸을 가능성).

### 매입금 (차감권)

샘플 반납·불량 매입·안 하기로 한 상품 등으로 거래처에 돈이 남는다.
`creditAdd` / `creditUse` / `creditExpiry` / `creditNote` 를 거래에 달아 두고
거래처별 원장으로 보여준다. **기한이 지나면 사라지는 돈**이라 D-day를 센다.

### DB

`transactions` 에 `cash_paid`(nullable) · `credit_add` · `credit_use` ·
`credit_expiry` · `credit_note` · `prev_balance` · `balance`(둘 다 nullable, 9/15) 를 더했다.
표에 칸이 없으면 `remote.upsertTxRows` 가 **없는 칸만 하나씩 빼고** 다시 보낸다(예전엔 새 칸 전부를 뺐다). 품목의 `kind` 는 `items` jsonb 안이라
칸이 따로 없다. `schema.sql` 을 다시 실행하면 붙는다(여러 번 해도 안전).

### 아직 안 한 것

- 미송을 품목명으로 맞물린다. 이름을 다르게 적으면 다른 품목이 된다.
  줄끼리 직접 연결(`linkId`)하는 자리는 데이터에만 비워 뒀다.
- 불량을 매입으로 정리할 때 교환 줄 + 매입금 칸을 따로 적어야 한다(두 번).

## 15. CSV·엑셀 넣기 (2026-09-09)

**카페24 CSV는 UTF-8이 아니라 EUC-KR이다.** 그냥 읽으면 머리글이 깨져서
'결제합계'를 못 찾고 화면에는 "못 읽었어요"만 뜬다. 실제로 이것 때문에 파일이
안 들어갔다. `src/lib/tabular.js` 가 UTF-8로 먼저 읽어 보고 깨지면 EUC-KR로 읽는다.
엑셀 파일(.xlsx/.xls)은 SheetJS로 CSV 텍스트를 만들어 넘긴다 —
**동적 import 라 엑셀을 안 넣으면 받지도 않는다**(별도 청크).

넣는 자리는 셋 다 된다: 끌어다 놓기 · 파일 고르기 · 붙여넣기.
광고비는 매일 넣는 것이라 **'데이터 넣기'에서 떼어 따로 버튼**을 뒀다.
'데이터 넣기' 안에 묻어 두니 안 보였다.


## 16. 삼촌비는 날짜별로 (2026-09-13)

삼촌에게 **월급을 주지 않고 그날그날 낸다.** 그래서 `DEFAULT_COSTS.samchon` 은 0이고,
실제 비용은 매출 장부 › **삼촌비 넣기**에서 날짜 + 금액으로 적는다 (광고비 넣기와 같은 모양).
**월 삼촌비 칸은 없애지 않았다** — 나중에 월급을 주게 되면 쓴다. 둘 다 있으면 손익에서 더한다
(`samchonDay = samchonMonthly + samchonDaily`).

저장은 `sales_daily` 칸이 아니라 **`settings` 의 `samchonDaily: {날짜: 금액}`** 이다.
칸을 새로 만들면 SQL을 또 돌려야 하고, 새벽 자동 갱신이 `sales_daily` 를 덮어쓰는 것과도
안 얽힌다. `settings` 는 한 덩어리라 둘이 동시에 고치면 덮어쓸 수 있으므로
`putSamchon` 은 **서버 최신을 읽어 그 날짜만 바꿔 쓰고**, 비용 칸을 저장하는 `saveConf` 는
**서버의 `samchonDaily` 를 그대로 보존**한다. 이 규칙을 깨면 상대가 적은 삼촌비가 사라진다.

자동 갱신 시각은 **새벽 1시**다. 사무실에 보통 1시까지 있고 4시엔 컴퓨터가 꺼져 있다.
카페24 숫자도 1시쯤이면 거의 다 들어와 있다. 꺼져 있었으면 켜진 뒤 바로 돈다.

## 17. 단가표 화면을 없앴다 (2026-09-14)

세원과 상의해서 뺐다. 이유 셋:

- **손익의 매출원가는 단가표를 안 쓴다.** 주문마다 붙어 온 공급가로 센다. 지워도 숫자가 안 바뀐다.
- 앱의 단가표는 **9/8에 박아 둔 241개**(`SEED_PRICES`)라 이미 낡았었다 (카페24는 263개).
- 마진표(판매가 정할 때 보는 것)는 세원이 **구글 시트 '고정 단가표'**에서 직접 한다.

단가표가 하던 일 중 **꼭 남겨야 하는 것 하나** — "공급가가 빠진 채 팔린 상품 찾기" — 만
**손익 화면 경고**로 옮겼다. 공급가가 비어 있으면 그날 원가가 0원으로 잡혀 이익이 부푼다.
새벽 자동 갱신(`daily.py` → `orders.missing_cost_items`)이 `settings` 의 **`missing_cost`** 키에
날짜별로 적고(본 기간 날짜만 갈아끼움), `PnlPage` 의 `MissingCost` 가 보고 있는 기간만 보여준다.
없으면 초록 한 줄("원가가 다 잡혔습니다")로 점검이 돌고 있다는 것만 알린다.
2026-08-01~09-14 기준 누락 0개.

## 18. 구글 시트 '고정 단가표' (앱 밖, 참고)

세원이 **판매가를 정할 때만** 쓰는 시트 (`1SHX4VNsDtZHipuGKZUZGL1z8Jvn_O3yXR4zSpjnC3lo`, 탭 `고정 단가표`).
같은 파일의 `주문 계산표`가 **`'고정 단가표'!$A$17:$P$754`(3열 판매가·6열 원가), `$B$4`, `$B$10`** 을
참조한다 — **A~P 열을 끼워 넣거나 B4/B10 값을 바꾸면 8월 주문 기록 계산이 같이 바뀐다.**

2026-09-14 세원 허락으로 고침: B3 PG 2%→**2.7%**(네이버 3.74% 평균), B11 배송원가 `=B4`→**2,300**
(B4 2,600은 그대로), J16 제목 "(E×6%)"→"(E×B3)", **B12 광고비율 18% / B13 부가세 1.5%** 추가,
**Q~T열**(P 오른쪽)에 광고비·부가세·광고 반영 영업이익·이익률 추가. 17~40행 평균 이익률 46.5% → 27.0%.
세원은 이 계산기를 **앱으로 옮길지 고민 중**이다(비용 가정값이 이미 앱에 있으므로).

## 19. 판매가 계산기 (2026-09-14)

구글 시트 '고정 단가표'를 앱으로 옮겼다 — 돈 › 매출 › **판매가 계산기** (`PricingPage`, `src/lib/pricing.js`).
세원: "상품별로 계산하고 그걸 하나씩 모아갈 수 있게."

- 공급가를 넣으면 판매가 후보(기본 판매가 앞뒤 1,000원 간격, 끝자리 800)마다 남는 돈·이익률.
  **목표 이익률(기본 25%)을 넘는 가장 싼 가격**을 짚는다. 고른 가격의 한 벌 손익을 풀어 보여주고 **목록에 담는다.**
- 규칙은 상품등록과 같다: 기본 판매가 = 공급가×2 끝자리 800 (`register.price_of`),
  정상가 = (판매가+3,000)×1.2 (`register.retail_of`). 한쪽을 바꾸면 둘 다 바꿀 것.
- **시트와 다른 점 — 가정값을 손으로 넣지 않는다.** 최근 30일(원가 아는 날) 실제 평균을 한 벌 기준으로 쓴다
  (`actualRates`): 배송비 수입·택배비 = 합계 ÷ 판매수량, 결제수수료율 = Σfee ÷ Σgoods.
  시트는 상품마다 배송비 3,000원을 더했는데 실제 한 벌당 수입은 **2,116원**이었다(8/16~9/14).
  광고비율은 **목표 18% / 최근 실제**를 고른다. 부가세는 간이 대략 1.5%. 반품은 반영하지 않는다(화면에 적음).
- 목록은 `settings` 의 **`pricing` 키** (`{items:[{id,name,vendor,supply,price,savedAt}]}`).
  삼촌비와 같은 방식으로 **서버 최신을 읽어 한 줄만 바꿔 쓴다**(`changePricing`). 가정값은 `conf.pricing`.
  settings 는 실시간 알림이 없어서 **탭을 다시 볼 때 다시 읽는다**(useSales 의 visibilitychange).
- 시트에 있던 상품 **293개를 옮겨 담았다**(판매가·원가 둘 다 있는 줄, id `sheet<행번호>`, `from: 구글 시트 고정 단가표`).
  목록에 저장되는 건 공급가·판매가뿐이라, 가정값을 바꾸면 전부 다시 계산된다.

### 판매가 계산기가 버벅이던 것 (2026-09-14, 재서 고침)

세원: "무슨 행동을 하면 엄청나게 렉이 걸려." 헤드리스 크롬 + CPU 4배 느리게 + 목록 293개로 쟀더니
**공급가 한 글자에 535ms**, 상품명 141ms 멈췄다. 원인과 고친 것:

1. 계산기에 한 글자 칠 때마다 **목록 293줄 전체를 다시 그렸다** → `SavedList` 를 `memo` 로 감싸고
   넘기는 값(settings·rates·핸들러)을 고정. `rates` 는 새로 읽어도 값이 같으면 예전 객체를 쓴다(JSON 비교).
2. 긴 표가 내용에 맞춰 칸 너비를 매번 다시 쟀다 → `table-fixed` + `colgroup`, **처음 60줄 + 더 보기**,
   화면 밖 목록은 `content-visibility:auto`.
3. `toLocaleString("ko-KR")` 이 부를 때마다 형식기를 새로 만든다 → `sales.won`·`EditNum` 이 `Intl.NumberFormat` 하나를 돌려 쓴다.
4. 가정값 칸은 치는 동안 화면에만, 멈추면 저장(`LazyNumber`). 창 포커스마다 전체를 다시 읽던 것은 15초에 한 번까지.

결과(같은 조건): 공급가 535→약 60ms, 상품명 141→19ms, 후보 누르기 152→31ms.
**목록 줄에 무거운 걸 더하거나 SavedList 에 매 렌더 새로 만드는 값을 넘기면 다시 느려진다.**

거래처 칸(`VendorInput`)은 그냥 쳐도 되고 매입 장부 거래처를 골라도 된다(마우스·↑↓·엔터).
고르면 `vendorId` 가 같이 저장되고 "매입 거래처" 표시가 붙는다. 손으로 고치면 연결이 떨어진다.

### 지원이 장끼를 넣다가 걸린 것 (2026-09-15)

1. **장끼 사진 읽기가 전잔·입금·당잔을 안 읽었다** → 당일합계만 들어가서 손으로 다시 적어야 했다.
   `api/read-receipt.js` 가 `prevBalance`·`cashPaid`·`balance` 도 읽는다(문자열로 받아 서버에서 부호 있는 숫자로).
   **△·▲·괄호도 마이너스.** supply 는 전잔을 더한 총계가 아니라 **그 날 산 것만**. 실제 리디아 장끼
   (전잔 −500 / 당일합계 17,500 / 현금입금 17,000 / 당잔 0)로 돌려서 네 칸 다 맞게 읽는 것 확인.
   장끼 아래 `매입전잔 / 매입잔액` 칸도 있다 — 아직 안 읽는다(매입금 기능과 이을 자리).
2. **전잔 −500을 적으면 "금액이 안 맞는다" 경고가 계속 떴다.** 앞선 장끼가 아직 안 들어갔거나 입금이 안 적혀
   앱이 전잔을 0으로 센 것뿐인데 오류처럼 보였다. → `balanceBasis`: 앞선 장끼에 입금·전잔·당잔이 **실제로 적혀
   있을 때만** 비교하고, 그때도 노란 경고가 아니라 회색 "참고" 한 줄. **장끼에 적힌 값이 기준이다.**
3. 결제방식 아래 "실제 지출"이 당일합계로 나왔다 → "매입 33,500 · 실제 건넨 돈 33,000".

**prev_balance / balance 칸이 없는 채로 쓰고 있었다**(SQL 미실행) — 그동안 적은 전잔·당잔은 저장이 안 됐다.
배포는 `registerType: autoUpdate` + 주입된 `registerSW.js` 라 **열려 있는 화면을 강제로 새로고침하지 않는다** —
작성 중인 장끼가 날아가지 않는다. 새 코드는 사용자가 새로고침해야 받는다.

### 불량이 합계에 드나 (2026-09-17 최종)

- **불량은 매입이다** — 합계에 든다. 교환받을 때 찾기 쉽게 표시해 둔 것일 뿐.
- **단, 미송으로 받은 물건에서 나온 불량은 뺀다.** 그 값은 미송 잡던 날 이미 냈다.
  같은 장끼에 **같은 품목의 '출고' 줄**이 있으면 `calc.isPrepaid(item, items)` 가 자동으로 뺀다.
  줄 아래 단추로 뒤집을 수 있다(`item.fromPaid` true/false, null이면 자동).
- 세원 화면(9/15 오도시)으로 검산: 아이보리 매입 24,000 + 블랙 출고 120,000(뺌) +
  아이보리 불량 12,000(듦) + 블랙 불량 12,000(출고분이라 뺌) = **36,000** = 장끼 당일합계.

## 20. 릴스 기획 (2026-09-17)

콘텐츠 › **릴스 기획** (`ReelsPage`, `api/reels.js`, `src/lib/video.js`, `src/lib/reels.js`).
세원: "레퍼런스 영상을 넣으면 대본을 뽑고, 우리 상품 URL을 붙이면 우리 릴스로 기획해주는 것."
참고한 것은 쇼필공 대표가 만든 **레퍼런스랩**(reels-lab.netlify.app).

### 먼저 알아야 할 제약

**Claude는 영상도 소리도 직접 못 읽는다.** 받는 건 글자·이미지·PDF뿐이다. 그래서:

- 브라우저가 영상에서 **장면 사진 8~14장**을 떠서 그것만 보낸다 (`extractFrames`, canvas).
  **영상 파일 자체는 아무 데도 안 올라간다.** 길이에 따라 장수를 정하고 긴 변 900px·JPEG 0.7로 줄인다.
- **자막이 박힌 릴스는 그대로 읽힌다** — 우리가 보는 레퍼런스 대부분이 이쪽이라 실용적이다.
- **목소리형은 소리를 못 듣는다.** 화면 형태를 고르면 '받아쓴 말' 칸이 열리고, 인스타 자동자막을
  복사해 넣으면 자막과 합쳐 정리한다. 안 넣으면 결과의 `note` 에 "소리는 못 들었다"고 적힌다.
- 인스타 **링크만 붙여넣는 방식은 안 된다**(로그인·스크래핑 차단). 영상 파일을 받는 쪽으로 만들었다.

### 흐름

1. **대본 뽑기**(`mode: "script"`) — 장면 사진 + 형태 + (선택) 받아쓴 말 → 제목·훅·대본·장면 흐름·
   구조(훅 방식/전개/CTA/왜 먹히는지). 구조화 출력(zod)이라 화면이 항상 같은 모양으로 그린다.
2. **우리 상품으로**(`mode: "adapt"`) — 위 대본·구조 + 상품 URL → 상품 분석(소구점·대상·주의)·훅·
   대본·**촬영 순서표**·인스타 본문·해시태그. 구조는 빌리고 내용만 우리 상품으로 바꾼다.
3. **라이브러리** — `settings` 의 `reels` 키. 판매가 목록과 같은 read-modify-write(`changeList`).

### 상품 페이지 읽기 (`readProduct`)

페이지 전체를 글자로 만들면 **메뉴가 절반**이라 상품 얘기가 묻힌다. 세 군데만 본다:
`og:title`(상품명) · `og:description`(요약설명) · `#span_product_price_text`(판매가,
정가는 `span_product_price_custom`) · **`#prdDetail` 이후**(MD코멘트·COLOR·FABRIC·사이즈·착용정보 —
우리가 등록할 때 넣은 글). '배송정보/결제정보' 뒤는 모든 상품 공통 안내라 자른다.

### 확인한 것 (2026-09-17)

헤드리스 크롬에서 캔버스로 **자막 박힌 가짜 릴스 6초**를 만들어 넣었더니 자막 세 줄을 그대로 읽고
제목·구조까지 뽑았다. 이어서 **395 와플 셔링 스트링 밴딩 팬츠** URL로 2단계를 돌려 42,800원·3color·
면 95%·총장 107cm 를 정확히 읽고 촬영 순서표까지 나왔다(41초).

- 프롬프트에 박아 둔 것: 자막은 **글자 그대로**(맞춤법 고치지 말 것), 없는 말 지어내지 말 것,
  과장 광고 문구 금지, **상품 글에 없는 소재·기능 지어내지 말 것**, 가격은 페이지 값만.
- `vite.config.js` 의 devApi 가 이제 `read-receipt` + `reels` 둘 다 개발 서버에 물린다.

### 라이브러리 · 폴더 · 반자동 기획 (2026-09-17 저녁)

세원이 레퍼런스랩 화면을 보여주며: "영상을 폴더로 모을 수 있는 공간과 **반자동으로 이뤄지는 기획**."
그래서 세 가지를 더했다.

1. **영상 보관** — Supabase Storage `reels` 버킷. 키는 `<기획id>`(영상)과 `<기획id>-thumb.jpg`(썸네일).
   썸네일은 `extractFrames` 가 **두 번째 장면**으로 만든다(첫 장면은 검은 화면·인트로가 많다).
   40MB 넘는 영상은 썸네일만 남기고 안내한다. 무거운 파일을 settings jsonb 에 넣으면 목록 전체를
   읽고 쓸 때마다 느려지므로 **파일은 Storage, 기획 내용은 settings** 로 갈랐다.
   **`schema.sql` 을 다시 실행해야 버킷이 생긴다.** 없으면 그 안내가 화면에 뜬다.
2. **폴더** — `item.folder`. 기본 갈래(미분류·판매형·정보성·코디릴스·관심끌기용) + 상세에서 새 폴더를
   쳐서 만든다. 폴더 목록은 items 에서 유도하므로 따로 저장하지 않는다(빈 폴더는 안 만들어진다).
3. **반자동 기획** — 분석 결과에 **빈칸 틀**이 들어간다.
   - `template` : 대본에서 상품마다 달라지는 자리를 `{{핵심소재}}` 로 판 것. 줄 앞에 `[0:03]`.
   - `slots` : `{key, hint, original}` — 그 자리에 뭘 넣는지와 레퍼런스에선 뭐라고 썼는지.
   - 상품 URL을 넣으면 `filled: [{key, value}]` 로 **빈칸이 자동으로 채워지고**, 화면에서 칩을 눌러
     직접 고칠 수 있다. 완성 대본은 `fillTemplate` 로 그때그때 합친다(`src/lib/reels.js`).
   - **조사는 빈칸 안에 넣는다.** 빈칸 밖에 두면 "허리 밴딩에 끈까지 있어**라** 조이는 느낌이" 처럼
     말이 어긋난다. 실제로 한 번 그렇게 나와서 프롬프트를 고쳤다(script·adapt 양쪽).
   - 구조분석도 레퍼런스랩처럼 **공감 포인트 / 후킹 공식(A·B) / 문장별 역할(후킹·본문·심리·CTA)** 로 넓혔다.

확인(2026-09-17): 가짜 릴스 → 라이브러리 담김 → 구조분석 3종 표시 → 395 상품으로 빈칸 자동 채움
("착용상황=하루종일 입을 / 핵심소재=허리 스트링이라 / 체감장점=내 허리에 딱 맞아요") → 폴더 '판매형' 저장까지.
