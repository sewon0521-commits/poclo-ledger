# 다음에 할 것

## 1. UI 다듬기

기능부터 붙인 상태라 화면이 투박하다. 참고: 쇼필공ERP 화면과 포클로 브랜드 톤
(로즈 포인트 + 뉴트럴 스톤, 깔끔하고 차분하게).

특히 손볼 곳:
- 거래처 카드 · 순위 목록의 여백과 위계
- 입력 폼이 길다 — 자주 쓰는 것과 가끔 쓰는 것을 나눌 것
- 거래처 합치기가 `window.prompt`다. 목록에서 골라 합치는 화면으로.
- 쇼필공ERP처럼 "① 올리기 → ② 송금" 같은 단계 표시가 있으면 지금 뭘 할 차례인지
  한눈에 보인다.

## 2. 홈택스 자동 수집 — 쇼필공ERP가 어떻게 하는지 알아냄

사장님이 보내준 쇼필공ERP 화면에 방법이 그대로 적혀 있다. **공동인증서를 앱에
넣는 게 아니다.** 두 조각으로 되어 있다:

### (1) 홈택스 '부서사용자' 계정
홈택스에서 **전자세금계산서 전용 부서사용자 아이디**를 만든다. 공동인증서 없이
아이디·비밀번호만으로 쓰는 홈택스 계정이고, 무료이며 여러 명이 동시에 쓸 수 있다.
대표 ID로 한 번 '승인'해주면 끝. 그래서 인증서를 어디에도 맡기지 않아도 된다.

### (2) 전자세금계산서 중개 서비스 (유료)
화면의 **LinkID · SecretKey**가 그 서비스의 인증값이다. 이 이름을 쓰는 곳은
**팝빌(Popbill)**이다. 팝빌의 홈택스 수집 API에 위 부서사용자 아이디를 등록해두면,
버튼 한 번으로 그 달 세금계산서를 끌어온다.

- 비용: 건당 과금 (수집 요청·조회 단위). 월정액 아님. 정확한 단가는 팝빌에서 확인.
- 우리가 붙일 자리: `api/` 아래 서버리스 함수 하나. LinkID·SecretKey는 **서버
  환경변수로만** 둔다(ANTHROPIC_API_KEY와 같은 방식).
- 주의: 홈택스 인증(부서사용자)과 연동 등록한 회사의 **사업자번호가 같아야** 한다.
  다르면 "사업자 번호가 존재하지 않습니다(-99003008)"가 뜬다.

### 그래서 어떻게 할까

**지금은 A(무료)부터.** 홈택스에서 "매입처별 세금계산서합계표"를 엑셀로 받아
올리면, 사업자번호·금액·날짜로 우리 거래와 맞춰 체크를 자동으로 켠다.
공짜이고 이미 만들어둔 거래처 매칭 로직(`src/lib/match.js`)을 그대로 쓴다.

**손이 많이 가면 B(팝빌)로.** 위 두 조각을 붙이면 버튼 한 번이 된다.

어느 쪽이든 **사업자번호가 열쇠**다. 거래처마다 사업자번호를 채워두는 게 먼저다.
지금 세금계산서 대조 화면은 앱에 기록한 '계산서 받음' 체크가 기준이라, 홈택스
실제 발행 내역과는 아직 대조하지 않는다.

## 3. M4 (원래 계획)

- CSV 일괄등록
- 삼촌 대납 정산 — 갚아야 할 현금 합계
- 매출·예상 부가세
