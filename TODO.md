# 다음 작업 요청사항

사장님이 2026-09-04에 주신 요청. 참고한 화면은 현재 쓰고 있는 다른 ERP(쇼필공ERP)의
거래처 목록 화면과, 장끼 입력 폼이다.

**아직 아무것도 구현 안 됨. 여기부터 시작하면 된다.**

---

## 1. 왼쪽 사이드바 (레이아웃 개편)

지금은 상단 탭(일별 / 거래처별) 구조. 이걸 왼쪽 사이드바로 바꾼다.

- 사이드바 메뉴 2개로 시작: **거래처**, **포클로 매입 장부**
- 지금 "거래처별" 탭에 있는 내용을 사이드바의 **거래처**로 옮긴다
- 참고 화면처럼 메뉴를 그룹(예: 사입·발주 / 정산·세무)으로 묶을 수 있게 여지를 둔다
- 모바일에서는 접히는 형태여야 한다(폰 한 손 사용이 기준)

## 2. 거래처 등록 · 거래처 목록

- 거래처를 **직접 등록**할 수 있어야 한다(장끼 없이도)
- 참고 화면처럼 카드형 목록: 거래처명 · 위치 · 전화 · 계좌 · 실제 이체액
- 검색(거래처명·위치·계좌번호·예금주·전화)
- 정렬: 이름순 / 거래액 많은순 / 최근 거래순
- 장끼를 올려 거래처가 자동 등록되는 흐름은 유지한다
- **장끼 등록 폼에서 이미 등록한 거래처를 토글(드롭다운)로 골라 넣을 수 있게** 한다
  (지금은 자동완성 목록만 있음)

### 나중에 볼 것 (참고 화면에 있던 것들, 이번 범위 아님)
- 같은 거래처가 두 개로 잡혔을 때 합치는 "매칭" 기능
- 사업자등록증 수집 상태(등록됨/미등록) 필터

## 3. 거래처 → 거래·순위

새 화면. 거래처 화면 안에서 **목록 / 거래·순위** 로 전환한다.

1. **날짜 선택**: 오늘 · 이번달 · 저번달 · 임의 기간(직접 지정)
2. 선택한 기간의 **총 거래액**과 **거래처 수**를 위에 표시
3. **거래액 높은 순**으로 거래처를 나열
4. 거래처를 누르면 → 다시 날짜를 고를 수 있고, 그 기간의 **영수증 금액(공급가 기준)** 이 나온다

## 4. 수동 입력 후 나중에 사진(장끼) 붙이기

미송건이라 장끼가 늦게 오거나, 나중에 거래처에 요청해서 받는 경우가 있다.

- 먼저 직접 입력해서 저장해두고
- 나중에 그 거래에 **장끼 사진을 추가**할 수 있어야 한다
- 장끼가 아직 없는 거래는 목록에서 눈에 띄게(예: "장끼 없음" 표시)

## 5. 입력 폼 개편 — 행 추가 / 삭제

두 번째 참고 이미지(장끼 입력 폼) 형태로 간다.

### 계좌번호
- 장끼에 **사업자 계좌와 일반 계좌가 따로** 적혀 있을 때가 있다
- "장끼 없이 직접 입력"에서 계좌를 **행 추가**할 수 있는 버튼
- 추가한 행은 **삭제**도 가능해야 한다
- 한 행 = 은행 / 계좌번호 / 예금주

### 품목
- 품목도 **행 추가 / 삭제**
- 한 행 = 품목명 / 단가 / 수량 / 금액 / **미송 버튼**
- 미송 버튼은 눌러서 켜고 끌 수 있어야 한다
- 품목 합계를 보여준다

### 장끼를 첨부하면
- 읽어온 품목·계좌가 **자동으로 행에 추가되어 채워진다**

### 참고 이미지에 있던 금액 구조
```
품목 합계        19,000원
당일합계         19,000        ← 직접 수정 가능
  부가세 미포함 19,000원 / 부가세 포함 20,900원
송금할 금액      19,000        ← 부가세 뺀 공급가로 입력
입금 계좌        은행 / 계좌번호 / 예금주
```

---

---

## 정해진 것 (2026-09-04 사장님 결정)

### 6. 거래처 식별 — 이름 말고 고유 번호로

거래처는 **고유 id**를 갖고, 거래는 그 id에 붙는다. 이름은 표시용일 뿐이다.
장끼마다 상호가 다르게 읽혀도(“ONE PICK” / “원픽” / “ONE PICK (원픽)”) 같은
거래처로 남게 하기 위해서다.

장끼를 읽은 뒤 기존 거래처와 맞춰보는 순서:

1. **전화번호나 계좌번호가 같으면 같은 거래처로 본다.**
   이름은 사진마다 흔들리지만 이 둘은 안 흔들린다. 가장 믿을 만한 열쇠다.
   → 폼에 “○○ 거래처로 잡았어요”라고 표시하고, 아니면 바꿀 수 있게 한다.
2. 번호는 안 맞고 **이름만 비슷하면** 후보를 보여주고 고르게 한다.
   (자동으로 합치지 않는다 — 다른 가게일 수 있다.)
3. 아무것도 안 맞으면 **새 거래처로 등록**한다.

거래처 칸은 **돋보기가 달린 토글**로, 눌러서 등록된 거래처를 검색해 고를 수 있어야
한다(사장님 제안 A). 자동으로 잡힌 게 틀렸으면 **직접 고쳐 쓸 수도** 있어야 한다
(제안 B). 둘 다 넣는다.

이미 둘로 나뉘어 버린 거래처를 나중에 **하나로 합치는(매칭)** 기능도 필요하다.
합칠 때 두 쪽 거래가 모두 한쪽으로 따라와야 한다.

### 7. 장부 금액은 "당일합계"

품목 합계와 당일합계가 다를 때(에누리 등) **당일합계를 장부 금액으로 쓴다.**
품목 합계는 참고용으로 옆에 보여주기만 한다.

### 8. 송금한 계좌 선택

장끼에서 계좌가 여러 개 읽히면(A·B·C) **그중 실제로 송금한 계좌 하나를 고를 수
있게** 한다. 고른 계좌는 그 거래 건에 기록된다.
- 계좌가 하나뿐이면 자동으로 그것이 선택된다
- 삼촌 대납처럼 계좌 송금이 아닐 때는 선택 안 해도 된다
